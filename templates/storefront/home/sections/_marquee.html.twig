{% if section.data.items %}<section class="cms-marquee" aria-label="{{ section.title }}"><div class="container">{% for item in section.data.items %}<span>{{ item }}</span>{% endfor %}</div></section>{% endif %}
