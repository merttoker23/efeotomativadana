<div class="navigation-band">
    <div class="container navigation-inner">
        <a class="departments-control" href="{{ path('storefront_catalog_index') }}">
            <span aria-hidden="true">☷</span>
            Tüm Kategoriler
            <small>{{ catalog_navigation.categories|default([])|length }}</small>
        </a>

        <nav class="main-navigation" aria-label="Ana navigasyon">
            <a href="{{ path('app_home') }}"{% if app.request.attributes.get('_route') == 'app_home' %} aria-current="page"{% endif %}>Ana Sayfa</a>
            <a href="{{ path('storefront_catalog_index') }}"{% if app.request.attributes.get('_route') starts with 'storefront_catalog_' and app.request.attributes.get('_route') != 'storefront_catalog_brands' %} aria-current="page"{% endif %}>Ürünler</a>
            <a href="{{ path('storefront_catalog_brands') }}"{% if app.request.attributes.get('_route') == 'storefront_catalog_brands' %} aria-current="page"{% endif %}>Markalar</a>
            <span aria-disabled="true">Kampanyalar</span>
        </nav>

        <div class="quick-navigation" aria-label="Hızlı erişim">
            <span aria-disabled="true">Favoriler</span>
            <span aria-disabled="true">Karşılaştır</span>
        </div>
    </div>
</div>

<nav
    id="mobile-navigation"
    class="mobile-navigation"
    aria-label="Mobil navigasyon"
    data-storefront-shell-target="menu"
    hidden
>
    <div class="container mobile-navigation-inner">
        <a href="{{ path('app_home') }}"{% if app.request.attributes.get('_route') == 'app_home' %} aria-current="page"{% endif %}>Ana Sayfa</a>
        <a href="{{ path('storefront_catalog_index') }}">Ürünler</a>
        <a href="{{ path('storefront_catalog_brands') }}">Markalar</a>
        {% for category in catalog_navigation.categories|default([])|slice(0, 5) %}<a href="{{ path('storefront_catalog_category', {slug: category.slug}) }}">{{ category.name }} <small>{{ category.productCount }}</small></a>{% endfor %}
        <a href="{{ path(is_granted('ROLE_CUSTOMER') ? 'customer_account_dashboard' : 'customer_login') }}">Hesabım <small>{{ is_granted('ROLE_CUSTOMER') ? 'Profil' : 'Giriş' }}</small></a>
    </div>
</nav>
